{
  "compilerOptions": {
    /* Projects */
    "incremental": true,                              /* Save .tsbuildinfo files to allow for incremental compilation of projects. */

    /* Language and Environment */
    "target": "ESNext",                               /* Use the latest ECMAScript features. */
    "module": "NodeNext",                             /* Set module system to NodeNext, matching ES modules. */
    "moduleResolution": "NodeNext",                   /* Node.js-style module resolution. */
    "rootDir": "./src",                               /* Specify the root folder within your source files. */
    "outDir": "./dist",                               /* Specify the output folder for all emitted files. */
    "esModuleInterop": true,                          /* Compatibility for CommonJS and ES modules. */
    "forceConsistentCasingInFileNames": true,         /* Ensure consistent file name casing. */
    "strict": true,                                   /* Enable all strict type-checking options. */
    "skipLibCheck": true,                             /* Skip type checking of declaration files. */

    /* JavaScript Support */
    "allowJs": true,                                  /* Allow JavaScript files to be a part of your program. */

    /* Emit */
    "sourceMap": true,                                /* Generate source maps for easier debugging. */
    "removeComments": true,                           /* Remove comments in the emitted code. */

    /* Interop Constraints */
    "resolveJsonModule": true,                        /* Enable importing JSON files. */
    "allowSyntheticDefaultImports": true,             /* Allow default imports from modules with no default export. */

    /* Completeness */
    "noEmitOnError": true                             /* Disable emitting files if there are any errors. */
  },
  "include": ["src/**/*.ts"],                          /* Include all TypeScript files in the src folder. */
  "exclude": ["node_modules", "dist"]                  /* Exclude node_modules and dist folders. */
}
